# In local dev, Vite's proxy (vite.config.ts) forwards /api to the backend,
# so no env var is needed there. In production on Vercel this is also not
# needed as long as frontend and backend are deployed as the SAME Vercel
# project (see the setup guide) — /api requests resolve to the Vercel
# serverless functions automatically.
